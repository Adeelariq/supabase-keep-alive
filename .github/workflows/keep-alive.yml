name: Test Project 3

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Project 3
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL_3 }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY_3 }}
        run: |
          curl -i \
            "$SUPABASE_URL/rest/v1/products?select=*" \
            -H "apikey: $SUPABASE_KEY" \
            -H "Authorization: Bearer $SUPABASE_KEY"
